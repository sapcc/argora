apiVersion: v1
kind: ConfigMap
metadata:
  name: config
data:
  config.json: |-
    {
      "ironCoreRoles": "{{ .Values.ironcore_roles }}",
      "ironCoreRegion": "{{ .Values.ironcore_region }}",
      "serverController": "{{ .Values.controller }}"
    }
